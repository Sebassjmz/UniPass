const deployContract = async () => {
    console.log('Deploy de contrato no implementado aún');
    console.log('Este script se usará cuando tengas el contrato de Stellar listo');
    process.exit(0);
};

deployContract();